/**
 * xLights networks.xml Generator
 *
 * Generates valid xlights_networks.xml files from controller configurations.
 * Used by the Shop Wizard to create starter controller configs.
 *
 * TODO: Implement
 * Inputs: Array of controllers with port assignments
 * Outputs: Valid xlights_networks.xml string
 */

import type { NetworkEntry } from "../types/controllers";

export function generateNetworks(_controllers: NetworkEntry[]): string {
  throw new Error("Not implemented — see docs/tickets/ for spec");
}
